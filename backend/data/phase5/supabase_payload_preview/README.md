# Phase 5A — Production Sync Payload Dry Run

**Status:** DRY RUN ONLY — no Supabase upload performed.  
**Generated by:** `backend/scripts/phase5_payload_dry_run.py`  
**Preview dir:** `backend/data/phase5/supabase_payload_preview/`  
**Migration (not executed):** `supabase/migrations/20260823_phase5a_production_fields.sql`

## A. Tables / objects to sync

| Object | Purpose |
|---|---|
| `company_stats` | One row per S&P ticker (502): identity, coverage, primary 10-Q NI, secondary revenue/10-K, ranking flags, compact `points` |
| `sector_stats` | 11 GICS sectors: dual-weight 10-Q NI (filing-weighted + company-balanced), revenue comparability flag |
| `example_filings` | Small featured case-study set (≤10 rows); capped sentences; no full MD&A HTML |
| `preload_status` | Not written in dry-run |

Not synced: MD&A text, filing HTML, full FinBERT sentence corpus, model files, quality logs, SQLite DBs.

## B–D. Counts & footprint

See `payload_summary.json` → `footprint`.

## E. Schema changes

Additive only. Apply `20260823_phase5a_production_fields.sql` **before** upload. Live DB currently lacks `stats_phase2` and Phase 5A columns. Do not drop legacy columns yet.

## F–L.

See `payload_summary.json` and chat Phase 5A report.
